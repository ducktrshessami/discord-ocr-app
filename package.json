{
  "name": "discord-ocr-app",
  "version": "0.1.0",
  "description": "A user-app for recognizing text in images with Tesseract",
  "type": "module",
  "main": "dist/index.js",
  "repository": "git@github.com:ducktrshessami/discord-ocr-app.git",
  "author": "ducktrshessami <ducktrshessami@duck.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rm -rf dist/ && tsc",
    "minify": "node minify.mjs",
    "minbuild": "yarn build && yarn minify",
    "start": "node --enable-source-maps --env-file-if-exists .env dist/index.js",
    "deploy": "node --enable-source-maps --env-file-if-exists .env dist/deploy.js"
  },
  "dependencies": {
    "@discordjs/core": "^2.6.0",
    "discord-data-resolvers": "ducktrshessami/discord-data-resolvers#v0.5.0",
    "fastest-levenshtein": "^1.0.16",
    "tesseract.js": "^7.0.0",
    "undici": "^8.10.0"
  },
  "optionalDependencies": {
    "bufferutil": "^4.1.0",
    "zlib-sync": "^0.1.10"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "minify": "^9.2.0",
    "typescript": "^7.0.2"
  }
}
